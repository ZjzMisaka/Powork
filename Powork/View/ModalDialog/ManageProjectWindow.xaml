<ui:FluentWindow
    x:Class="Powork.ManageProjectWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Powork"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    Title="{DynamicResource ManageProject}"
    Width="600"
    Height="300"
    d:DataContext="{d:DesignInstance local:ManageProjectWindow,
                                     IsDesignTimeCreatable=True}"
    d:DesignHeight="400"
    d:DesignWidth="600"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    ExtendsContentIntoTitleBar="True"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    WindowBackdropType="Mica"
    WindowCornerPreference="Round"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d" Cursor=""
    ScrollViewer.CanContentScroll="False">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Other Users"/>
        <ListBox Grid.Row="1" Grid.Column="0" ItemsSource="{Binding OtherUsers}" SelectedItem="{Binding SelectedUser}" DisplayMemberPath="Name"/>

        <StackPanel Grid.Row="1" Grid.Column="1" VerticalAlignment="Center">
            <Button Content="&gt;&gt;" Command="{Binding AddManagerCommand}" Margin="5"/>
            <Button Content="&lt;&lt;" Command="{Binding RemoveManagerCommand}" Margin="5"/>
        </StackPanel>

        <Label Grid.Row="0" Grid.Column="2" Content="Managers"/>
        <ListBox Grid.Row="1" Grid.Column="2" ItemsSource="{Binding Managers}" SelectedItem="{Binding SelectedManager}" DisplayMemberPath="Name"/>

        <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Exit Project" Command="{Binding ExitProjectCommand}" Width="100" Margin="5"/>
            <Button Content="OK" Click="OkButton_Click" Width="75" Margin="5"/>
            <Button Content="Cancel" IsCancel="True" Width="75" Margin="5"/>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
